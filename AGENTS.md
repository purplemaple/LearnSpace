# LearnSpace 笔记整理规则

本文是整理本仓库笔记与配套代码说明时的唯一执行基准。

## 1. 仓库定位

本仓库是个人编程知识库，重点不是做成正式教程，而是保留学习、验证、复盘过程。

整理内容时应服务于三个目标：

1. 让问题背景更清楚
2. 让方案边界更明确
3. 让后续回看时能快速判断某种写法能不能用、为什么能用、风险在哪里

不要把笔记改成百科式说明，也不要把个人判断全部抹平。

## 2. 目录关系

- `Code` 存放示例代码
- `Note` 存放对应 Markdown 笔记
- 笔记和代码路径基本对应，但允许存在暂缺文档或暂缺代码的情况

整理笔记时，如果存在对应代码，应优先保留或补充“对应示例”链接。

链接格式参考：

```markdown
对应示例：[Program.cs](../../../Code/...)
```

## 3. 行文风格

### 3.1 基本语气

使用第一人称学习笔记的语气，但不要写成日记。

推荐表达方式：

- “这里需要注意的是...”
- “此时会引发一个问题...”
- “这其实也是比较推荐的使用方式...”
- “我们可以发现...”
- “这种写法能用，但...”

避免表达方式：

- 过度正式的教材腔
- 没有边界的绝对化结论
- 空泛赞美
- 与当前主题无关的扩展

### 3.2 判断优先于罗列

笔记中可以列方案，但每个方案都要说明：

1. 解决了什么
2. 没解决什么
3. 有什么风险
4. 适合什么场景

不要只堆 API 用法。

### 3.3 保留“先结论后展开”

对于复杂主题，优先使用下面的顺序：

```markdown
## 1. 问题背景

## 2. 总结 / 总结先行

## 3. 方案一：...

## 4. 方案二：...

## 5. 方案对比 / 补充说明

## 6. 结语

## 7. 参考
```

如果主题较短，可以减少章节，但不要打乱“问题 -> 结论 -> 展开 -> 边界”的主线。

## 4. Markdown 格式习惯

### 4.1 标题

使用数字标题组织结构。

常见格式：

```markdown
## 1. 问题背景
## 2. 总结
## 3. 方案一：AsyncLock
### 3.1 使用回调委托处理善后工作
```

标题允许带问号，例如：

```markdown
## 1. 问题背景？
```

但同一篇内尽量保持一致。

### 4.2 分隔线

大章节之间可以使用：

```markdown
---
```

分隔线用于切开“总结、方案、结论”等大段落，不要在很短的小节之间滥用。

### 4.3 列表

解释步骤时使用数字列表：

```markdown
思路：
1. 使用 `Thread` 运行长耗时同步方法
2. 使用 `Thread.Interrupt` 强制打断
3. 使用信号量等方式暴露一个异步任务以供等待
```

解释优缺点、注意点时使用短列表：

```markdown
优点：
- 不阻塞当前线程
- 写法简单
```

可以保留缩进式补充点，但不要让缩进层级过深。

### 4.4 引用块

引用块用于强调关键判断、原理或结论。

示例：

```markdown
> 在同步方法中非阻塞地调用异步方法本身往往意味着设计受限。
```

`> info` 可以保留，用于补充背景知识或参考信息。

### 4.5 代码块

代码块必须注明语言。

常用语言标记：

```markdown
```csharp
```

```xml
```

```PowerShell
```
```

代码块前应先说明“典型写法”、“调用者”、“简单示例”、“自定义类”等上下文。

代码中可以保留必要中文注释，尤其是用于解释学习重点、风险点和设计意图的注释。

### 4.6 表格

多个方案对比时可以使用表格。

表格列数不要太多，优先包含：

- 方案
- 是否阻塞
- 异常是否容易处理
- 适用场景

## 5. 内容组织规则

### 5.1 每篇笔记应先交代问题

不要直接从 API 开始。

推荐开头：

```markdown
## 1. 问题背景

某些情况下...
```

问题背景要说明“为什么需要这个知识点”，而不是只解释名词。

### 5.2 总结要靠前

复杂笔记应在前部给出“总结”或“总结先行”。

总结可以直接判断：

- 不推荐
- 过渡可用
- 特定场景可用
- UI 场景下更推荐
- 只有性能分析证明需要时才使用

### 5.3 方案说明要有边界

每个方案尽量包含：

```markdown
### 优点

### 缺点

### 关键注意点

### 结论
```

不需要机械补齐所有小节，但风险和适用场景必须交代清楚。

### 5.4 保留“尝试失败”的过程

如果某个方案无效，应明确写出为什么无效。

示例：

```markdown
### 2.1 尝试使用 Task.Run 包装同步方法并传入 CancellationToken（此方法无效）
```

这种失败路径很有价值，不要为了“整洁”删除。

### 5.5 保留学习中的补充点

可以使用“补充点一”、“补充点二”等写法。

适合记录：

- 框架特性
- IDE 操作
- 特性 Attribute
- API 重载
- 与主线相关但不宜打断正文的知识点

### 5.6 结语要收束判断

结语不是重复全文，而是给出最终使用建议。

推荐形式：

```markdown
## 6. 结语

- ...
- ...
```

或：

```markdown
> ...
```

## 6. 技术判断偏好

### 6.1 C# / WPF 笔记

整理 C#、WPF、XAML、异步、多线程相关内容时，应优先强调：

- 线程是否阻塞
- 异常能否被正确观察和处理
- 生命周期是否受控
- 是否符合 UI/MVVM 模型
- 是否存在死锁、资源泄漏、线程安全问题
- 是否只是过渡方案

### 6.2 异步主题

异步相关笔记中，应保持以下判断口径：

1. 能异步到底时，优先异步到底
2. 同步里强行调异步通常意味着设计受限
3. Fire and Forget 需要处理异常、生命周期和完成时机
4. `CancellationToken` 只是协作式取消，不是强制杀任务
5. 超时机制通常只是提前返回，不等于取消原任务
6. `ValueTask` 不是 `Task` 的值类型优化版，而是特定场景工具
7. UI 场景优先让命令、事件、绑定承接异步生命周期

### 6.3 示例代码说明

说明代码时不要只解释语法，应说明这段代码承担的职责。

推荐表达：

```markdown
`TaskCompletionSource` 为一次性信号量，外部 `await` 等待的就是它。
```

```markdown
`Process` 具体执行第三方类库的哪个方法由构造中的 arguments 决定。
```

## 7. 修改边界

整理个人资料库时，应优先改 Markdown 文档。

不要随意重构示例代码。只有在文档引用路径明显错误、代码名称明显不一致，或用户明确要求时，才修改代码。

不要批量删除文件或目录。

禁止使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件。

如果需要批量删除文件，应停止操作，并询问用户手动删除。

## 8. 提交前检查

整理完成后应检查：

1. 标题层级是否连续
2. 对应示例链接是否存在
3. 代码块语言是否标注
4. 结论是否过度绝对
5. 是否把失败方案的原因讲清楚
6. 是否把生成物、`.vs`、`bin`、`obj` 当成正文资料处理

如涉及 Git，只报告真实状态，不自动丢弃用户改动。
